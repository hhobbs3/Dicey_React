Connects to AWS components and graphQL via mockup

To mock up graphQL, run: npx json-server --watch data/db.json --port 8000
    Note that the graphQL functionality has been temporarily disabled in graphQLExample.json

To run the program: npm run start

Recently added Tailwind functionality, causing much of the original css to not work as it once did.